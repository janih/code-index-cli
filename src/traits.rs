//! Core traits shared across modules.
//!
//! Port of `src/interfaces/*`: `Embedder`, `VectorStore`, `CodeParser`,
//! `CacheManager`. Filled in during Phase 1 (async via `async-trait` or
//! native AFIT once methods are defined).
